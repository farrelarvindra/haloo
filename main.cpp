#include <iostream>
#define ll long long
using namespace std;

int main() {
  ll n;
  cin >> n;

  pair<ll, ll> j[100005];
  for (int i = 0; i < n; i++) {
    cin >> j[i].first >> j[i].second;
  }
  sort(j, j + n);
  for (int i = 0; i < n; i++) {
  }
}
